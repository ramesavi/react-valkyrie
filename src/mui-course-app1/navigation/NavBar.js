import React from "react"
import { AppBar, Toolbar, Typography } from "@material-ui/core"

const NavBar = () => <div>
  <AppBar>
    <Toolbar>
      <Typography variant="h6">React & Material-UI Sample Application</Typography>
    </Toolbar>
  </AppBar>
</div>


export default NavBar